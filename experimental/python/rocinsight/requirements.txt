# rocinsight — full installation (LLM providers + rich output)
# Equivalent to: pip install -e ".[all]"
#
# Core analysis works with stdlib only (zero mandatory deps).
# Install this file to enable all optional features.

# LLM providers
anthropic>=0.18
openai>=1.0

# Rich terminal output
rich>=13.0

# claude-code provider: Claude Code CLI stored credentials
# Install if you want --llm claude-code without an ANTHROPIC_API_KEY.
# Requires the Claude Code CLI to be installed separately.
# claude-agent-sdk>=0.1
